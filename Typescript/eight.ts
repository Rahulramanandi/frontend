function multiplyNumbers(a: number, b: number): number {
    return a * b;
}

const multiplyNumbersArrow = (a: number, b: number): number => a * b;

console.log(multiplyNumbers(3, 4));
console.log(multiplyNumbersArrow(3, 4));
